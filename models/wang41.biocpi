species R = r -> 0;

species S = s -> (S | R);

species A = a -> (A | R);

affinity network Aff = {
  r at rate MA(5);
  s at rate MA(1);
  a at rate MA(0.01);
}

process P = [1.0] A || [1.0] S with network Aff;
